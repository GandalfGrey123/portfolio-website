<div class="container mt-5 h-100 d-flex justify-content-center">

    <div class="jumbotron my-auto">
      <h1>Thank you, </h1>
      <h2>your message has been successfully sent! </h2>
      	<div class="card mt-5 p-2 w-100 bg-light ">
          <h4 class="p-2">Message Preview: </h4>
  		  <div class="card-body">
  		    <h5 class="text-weak"> Email: <%= @show_message.senderEmail %></h5>
  		    <h6 class="card-subtitle mb-2 text-muted text-weak">Subject: <%= @show_message.title %></h6>

  		    <% if (@show_message.first || @show_message.last) %>

  		       <h6 class="card-subtitle mb-2 text-muted">Your Name: <%= @show_message.first %> &nbsp; <%= @show_message.last %></h6>
  		    <%end%>

  		    <p class="card-text">
  		    	 <%= @show_message.message %> 
  		    </p>

  		  </div>
		</div>

          <button type="button" class="mt-3 btn btn-light float-right">
            <%= link_to "Go Back", root_path %>            
          </button>
    </div>
</div>